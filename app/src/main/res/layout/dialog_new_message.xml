<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="wrap_content"
              android:layout_marginTop="10dp"
              android:background="@drawable/dialog_prompt"
              android:orientation="vertical">

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@color/colorTitleBlack"
        android:paddingBottom="5dp"
        android:paddingLeft="10dp"
        android:paddingTop="5dp"
        android:text="提示"
        android:textColor="@color/colorTitleWhite"
        android:textSize="22sp"
        android:visibility="gone"/>

    <Button
        android:id="@+id/btn_close"
        style="@style/dialog_finish"/>

    <TextView
        android:id="@+id/tv_content"
        style="@style/dialog_top_title"
        android:layout_marginTop="60dp"
        android:maxLines = "100"
        android:text="我是一条展示的信息！"/>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_marginBottom="30dp"
        android:layout_marginLeft="40dp"
        android:layout_marginRight="40dp"
        android:layout_marginTop="50dp">

        <Button
            android:id="@+id/btn_sure"
            style="@style/myBottomButton"
            android:layout_width="200dp"
            android:text="确定"/>

    </RelativeLayout>
</LinearLayout>