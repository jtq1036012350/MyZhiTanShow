<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <item android:id="@android:id/background">

        <shape>

            <corners android:radius="5dip" />

            <gradient
                android:angle="0"
                android:centerColor="@color/colorTitleWhite"
                android:centerY="0.75"
                android:endColor="@color/colorTitleWhite"
                android:startColor="@color/colorTitleWhite" />
        </shape>
    </item>

    <item android:id="@android:id/secondaryProgress">

        <clip>

            <shape>

                <corners android:radius="5dip" />

                <gradient
                    android:angle="0"
                    android:centerColor="@color/colorBlue"
                    android:centerY="0.75"
                    android:endColor="@color/colorBlue"
                    android:startColor="@color/colorBlue" />
            </shape>
        </clip>
    </item>

    <item android:id="@android:id/progress">

        <clip>

            <shape>

                <corners android:radius="5dip" />

                <gradient
                    android:angle="0"
                    android:endColor="@color/colorBlue"
                    android:startColor="@color/colorBlue" />
            </shape>
        </clip>
    </item>

</layer-list>  